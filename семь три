#include<iostream>
#include<string> 
using namespace std;

int main() {
	int count = 1;
	string s; 
	cout << "Enter the string: ";
	getline(cin, s); 
	for (int i = 0; i < s.size(); ++i) { 
	    if(s[i] == ' '){
	        count += 1;
	    }
	    
	}
	cout << count; 
	return 0;
}
